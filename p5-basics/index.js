console.log("In client.js")

//creates the canvas we are drawing on
function setup() {
    createCanvas(400, 400);
}

//sets the color
function draw() {
    background(100,100);
    fill(255, 204, 0);
    rect(100,50,10,10)
}